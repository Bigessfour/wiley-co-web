# Simple local RAG for agents (codebase + docs context)
# Run: python -m pip install -r Scripts/rag/requirements.txt
chromadb>=0.5.0
sentence-transformers>=3.0.0
pathspec>=0.12.0   # robust .gitignore-style filtering (pure python)